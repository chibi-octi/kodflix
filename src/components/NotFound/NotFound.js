import React from 'react';
import './NotFound.css';


export default function NotFound() {
    return (
        <div className="not-found">
            <div>
                Create a new NotFound component, and return an error message 
                (e.g. Ooops, it looks like this page doesn't exist :( 
            </div>
         
        </div>
    )
}